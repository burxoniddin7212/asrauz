export const PORT = 7006;

export enum RolesNames {
  user = 'user',
  admin = 'admin',
  superAdmin = 'superAdmin',
}

export enum RolesNamesOnlyAdmin {
  admin = 'admin',
  superAdmin = 'superAdmin',
}

export enum OrdersStatus {
  received = 'received',
  confirmed = 'confirmed',
  ordered = 'ordered',
  arrived = 'arrived',
  canceled = 'canceled',
}

export enum Gander {
  male = 'male',
  famele = 'famele',
}

export enum RowsStatus {
  active = 'active',
  delete = 'delete',
}

export const uzbTimeZone = 'Asia/Tashkent';
